import React from 'react';

import classes from './Footer.module.css';

const Footer = props => {
    return (
        <div className={classes.Footer}>
            <div className={classes.MockUpTitle}>FOOTER</div>
        </div>
    );
};

export default Footer;